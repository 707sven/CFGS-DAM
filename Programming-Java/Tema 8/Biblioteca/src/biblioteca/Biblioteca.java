/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package biblioteca;

/**
 *
 * @author usuario
 */
public class Biblioteca {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        /*Hacer una biblioteca que contenga libros y revistas
            > libro = codigo, titulo y año de publicacion
                - las 3 se pasan por parametro antes de crear los objetos
                - tienen además un atributo "prestado"
                - los libros cuando se crean no están prestados
            > revistas = tienen un id
                - no se puede prestar
            - las revistas y los libros tienen un toString() que devuelva el valor de sus atributos como una cadena
            - tendrá un metodo que devuelve el año de publicacion y otro para el codigo
        Para prevenir posibles cambios, se introduce una interfaz "Prestable" con los metodos prestar(), devolver() y prestado(). La clase libro la implementa.
        */
    }
    
}
